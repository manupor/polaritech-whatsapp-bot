# ── WhatsApp Cloud API ────────────────────────────────────────────────────────
# Use Meta test number + test token for staging.
# Use real business number + permanent token for production.
WHATSAPP_ACCESS_TOKEN=EAAxxxxxxx...
WHATSAPP_PHONE_NUMBER_ID=123456789012345
WHATSAPP_VERIFY_TOKEN=my_secret_verify_token
META_API_VERSION=v21.0

# ── LLM Provider (OpenAI or Anthropic) ────────────────────────────────────────
# Choose: openai or anthropic
LLM_PROVIDER=openai
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key
LLM_MODEL=gpt-4o-mini  # or claude-3-haiku for Anthropic
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=500

# ── App settings ─────────────────────────────────────────────────────────────
# Values: development | staging | production
APP_ENV=development
LOG_LEVEL=info

# ── Database ─────────────────────────────────────────────────────────────────
# SQLite works locally only. Serverless platforms (Vercel) have an ephemeral
# filesystem, so SQLite data is lost on every cold start and the bot loses
# conversation context. Use PostgreSQL (Neon / Supabase) in deployments:
#   postgresql://user:pass@host/dbname?sslmode=require
DATABASE_URL=sqlite:///polaritech.db

# ── Welcome flow ─────────────────────────────────────────────────────────────
WHATSAPP_WELCOME_IMAGE_URL=https://your-cdn.com/polaritech-cotizacion.jpg
WHATSAPP_WELCOME_IMAGE_ID=
WELCOME_WINDOW_HOURS=24.0

# ── Escalation ───────────────────────────────────────────────────────────────
ESCALATION_PHONE_NUMBER=+50688001234
